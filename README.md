# Handwritten-Digit-Recognition
IntelliDigit is an AI-based web application that recognizes handwritten digits using a deep learning model. It is built with TensorFlow and provides an interactive interface using Streamlit.  Users can draw a digit or upload an image, and the system processes it using OpenCV to predict the correct number along with a confidence score. 
